node_modules/
.DS_Store
*.log
.npmrc

# Maintenance kit (Squad) installed under .claude/ — personal tooling, never versioned
.claude/

# Personal maintenance registry (Squad) — not project documentation
BACKLOG.md

# Stryker outputs — a mutation report is evidence for one run, not repository content
reports/
.stryker-tmp/

# SDD workspace — execution scratch, not project content
.superpowers/

# Coverage output — `npm run test:coverage` writes coverage/lcov.info for the SonarCloud scan.
# It is a measurement of one run, not repository content, and the CI job asserts a clean tree.
coverage/
