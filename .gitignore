node_modules/
.DS_Store
*.log
.npmrc

# Kit de manutencao (Squad) instalado em .claude/ — nao versionado
.claude/

# Registro de manutencao pessoal (Squad) — nao e documentacao do projeto
BACKLOG.md

# Stryker outputs — a mutation report is evidence for one run, not repository content
reports/
.stryker-tmp/
